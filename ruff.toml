line-length = 100
target-version = "py310"

exclude = [".venv", "docs"]

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4", "SIM"]
ignore = ["E501"]

[lint.per-file-ignores]
"tests/*" = ["E402"]

[format]
# 코드베이스가 작은따옴표를 쓰므로 포매터도 맞춘다.
quote-style = "single"
